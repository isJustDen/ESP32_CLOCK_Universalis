// // Тест RTC модуля DS3231

// // Подключаем необходимые библиотеки
// #include <Wire.h>      // Библиотека для работы с шиной I2C
// #include <RTClib.h>    // Библиотека для работы с RTC

// // Создаём объект для работы с нашим модулем
// RTC_DS3231 rtc;

// void setup() {
//   Serial.begin(115200);
//   Serial.println("\nНачинаем тест RTC DS3231");

//   // Инициализируем I2C шину на стандартных пинах (SDA = 21, SCL = 22)
//   Wire.begin();

//   // Пытаемся инициализировать RTC модуль
//   if (!rtc.begin()) {
//     Serial.println("Ошибка: RTC модуль DS3231 не найден!");
//     Serial.println("Проверьте подключение по I2C.");
//     while (1); // Бесконечный цикл, если модуль не найден
//   }

//   // После успешного запуска настраиваем время, если батарейка на модуле новая
//   // Мы просто установим его во время компиляции этого скетча
//   // rtc.adjust(DateTime(F(__DATE__), F(__TIME__)));

//   // Для теста давайте установим конкретное время: 10 мая 2026 года, 15:30:00
//   // rtc.adjust(DateTime(2026, 5, 10, 15, 30, 0));

//   // Если RTC потерял питание (батарейка вставлена впервые), нужно установить время.
//   if (rtc.lostPower()) {
//     Serial.println("RTC потерял питание! Устанавливаем время по времени компиляции...");
//     // Эта функция устанавливает время в RTC на основе того, когда вы КОМПИЛИРУЕТЕ этот скетч.
//     // Это удобно для первичной настройки.
//     rtc.adjust(DateTime(F(__DATE__), F(__TIME__)));
//     // Но для точного времени лучше будет установить его из NTP, но это позже.
//   }
// }

// void loop() {
//   // Получаем текущее время и дату с RTC модуля
//   DateTime now = rtc.now();

//   // Выводим данные в Serial-порт
//   Serial.print("Текущее время: ");
//   Serial.print(now.year(), DEC);
//   Serial.print('/');
//   Serial.print(now.month(), DEC);
//   Serial.print('/');
//   Serial.print(now.day(), DEC);
//   Serial.print(' ');
//   Serial.print(now.hour(), DEC);
//   Serial.print(':');
//   Serial.print(now.minute(), DEC);
//   Serial.print(':');
//   Serial.print(now.second(), DEC);
//   Serial.println();

//   // Выводим температуру со встроенного в модуль датчика
//   Serial.print("Температура чипа: ");
//   Serial.print(rtc.getTemperature());
//   Serial.println(" C");

//   // Задержка перед следующим чтением
//   delay(1000);
// }