// // Тест датчика освещённости TEMT6000

// #include <Arduino.h>

// // Определяем, к какому аналоговому пину подключен датчик
// const int LIGHT_SENSOR_PIN = 35; // Используем GPIO35

// void setup() {
//   // Запускаем последовательную связь для вывода информации на компьютер
//   Serial.begin(115200);
//   Serial.println("\nНачинаем тест датчика TEMT6000");

//   // Настраиваем пин для встроенного светодиода на выход
//   pinMode(LED_BUILTIN , OUTPUT);
//   // Для аналогового пина настройка не нужна
// }

// void loop() {
//   // 1. Читаем значение с аналогового пина
//   // analogRead() вернёт число от 0 до 4095 (12 бит).
//   // 0 = 0 Вольт (очень темно), 4095 = 3.3 Вольт (очень светло).
//   int rawValue = analogRead(LIGHT_SENSOR_PIN);

//   // 2. Выводим сырое значение в монитор порта
//   Serial.print("Raw value from TEMT6000: ");
//   Serial.println(rawValue);

//   // 3. Небольшая демонстрация: включаем и выключаем светодиод,
//   //    чтобы визуально наблюдать, работает ли скетч.
//   //    Он просто моргает, не обращая внимания на показания датчика.
//   digitalWrite(LED_BUILTIN, HIGH);
//   delay(500);
//   digitalWrite(LED_BUILTIN, LOW);
//   delay(500);
// }